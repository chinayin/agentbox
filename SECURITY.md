# 安全问题报告

发现镜像、entrypoint、compose 模板或 CI 里的安全问题，请通过 GitHub 的 Security Advisories（仓库 Security 标签页 → Report a vulnerability）私下报告，不要开公开 issue。

报告里不要贴任何明文密钥。已泄露凭据的应急步骤见 `docs/SECRETS.md` §6。

只支持最新发布的版本 tag；旧版本不回补。
