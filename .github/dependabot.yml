# Only GitHub Actions; tool versions live in mise.lock and are bumped by lock.yml.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    labels: [dependencies]
